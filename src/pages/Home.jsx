import React from 'react';
import Navbar from './shared/Navbar';

const Home = () => {
    return (
        <div className="relative h-screen">
            
        </div>
    );
};

export default Home;